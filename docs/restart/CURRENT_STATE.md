# Current State

- Current project goal: Build Arangur v2 as a thin end-to-end demo system with replaceable component boundaries.
- Current active workstream: The local workflow/report demo is working, Education-stack alignment has been captured, the FastAPI app serves a lightweight catalog-driven Report Element Finder with dynamic template-specific configuration and manual narrative elements, the Report Element Finder / Composer current model is documented, a static report-element template catalog/API exists, a first optional Postgres persistence skeleton now covers workflow run metadata and draft briefing spec sets, and the advisor-facing browser composer now opens on a sparse Advisor Home before the build -> preview -> edit flow. The static app uses hash routes for Advisor Home, Builder, Client Preview, Advisor Preview, and Advanced so browser Back stays inside the product path. Non-empty Client Briefing Set and Advisor Review Set lists open focused previews with top-visible Back to Builder, Print, Export HTML, Copy text, and a compact demo caveat; selected manager/account/sleeve/strategy scopes now reveal a required selected-entity field and preview labels carry that entity in advisor-facing language. Local JSON transfer, backend draft save/load, and QA reference previews are demoted into Advanced. Saved draft briefing spec sets are implemented only as optional metadata/raw-payload persistence; generated reports from local composer specs and full client-ready report generation are not implemented yet.
- Current repo status observed: Arangur v2 has restart docs, thin demo contracts, simulation kernel architecture/contracts, synthetic local JSON fixtures, a standard-library Python demo pipeline under `src/arangur/`, a standard-library simulation package under `src/arangur/simulation/`, deterministic synthetic position universe, market-state, simplified valuation fixtures, report-element input/view fixtures and briefing-set preview fixtures under `data/simulation/`, focused tests, generated demo outputs under `reports/demo/`, and a minimal FastAPI app under `src/arangur/app/`. The dependency-free browser composer starts at Advisor Home, fetches `/api/report-elements` in the Builder workspace, keeps the client context to one compact line, uses search/category/topic inputs plus an attached compact grouped `Browse all templates` picker, shows a template preview before configuration, renders only meaningful selected-template fields, supports manual narrative elements, adds compact local specs to separate Client Briefing Set and Advisor Review Set lists, labels matching analytic specs as previewable, keeps one-element preview as a secondary element tool, copies/downloads/restores non-empty local set JSON with schema `arangur.local_briefing_spec_set.v1` from Advanced, previews current unsaved ordered sets in browser using existing rendered fragments plus nontechnical placeholders, exposes preview-only print/export/copy controls after a non-empty current preview is opened, optionally saves/loads draft local spec-set payloads through Advanced backend controls, keeps QA reference preview links under Advanced, and still does not call workflow APIs or show report-package artifacts in the composer path. Hash routes cover `#home`, `#builder`, `#preview/client`, `#preview/advisor`, and `#advanced`. The prior fixed-question, dense briefing-set, and card-style catalog UIs should be treated as superseded. Optional Postgres persistence for `workflow_run`, `report_artifact`, `run_event`, `briefing_spec_set`, and `briefing_spec_item` remains available when `DB_ENGINE=postgres` and `DATABASE_URL` are set. Default local mode remains `DB_ENGINE=none`, file-backed for workflow runs, and not-configured/no-op for briefing spec-set save attempts.
- Last completed batch: Advisor workflow shell, hash-route browser navigation, dependent selected-scope configuration, tests, and docs, completed 2026-06-30.
- Next intended batch: Run a Docker/private-demo packaging batch if Frank wants Postgres-backed save/load in a local private-demo stack, or ask Frank/ChatGPT for the next bounded product prompt. Do not add charts, live data, production auth, production client-ready report generation, or unapproved Docker changes outside an explicitly authorized follow-up batch.
- Current implementation status for the simulation kernel: Surfaces 1, 2, and 3 are implemented, their outputs feed the first report-element input payloads, those inputs render to simple view JSON plus Markdown/HTML fragments, those views compose into default Client Briefing Set and Advisor Review Set previews, and the browser composer can inspect those pre-rendered views when a local spec matches. Generated simulation report packages from local specs are not implemented yet; local browser export/restore is implemented for demo spec sets, and optional backend persistence can save/load draft spec-set payloads without generating reports.
- Open decisions for Frank: Confirm the actual private-demo subdomain, whether first browser protection should be token-only or cookie/session plus CSRF, and whether generated artifacts should start in a Docker volume or host-mounted app artifact directory.
- Known stop conditions: Do not use real client data; do not inspect credentials or secrets; do not call external APIs; do not integrate live Plaid; do not inspect or port legacy MATLAB without a specific authorized audit batch.
- Files a restarted model should read first: `docs/restart/00_READ_ME_FIRST.md`, then the remaining `docs/restart/` files in numeric order, then `CURRENT_STATE.md`; implementation batches should also read `docs/ui_reporting/`, especially `docs/ui_reporting/report_element_finder_composer_current_model_v1.md`, `docs/ui_reporting/briefing_set_builder_model_v1.md`, `docs/ui_reporting/briefing_set_report_view_model_v1.md`, `docs/ui_reporting/briefing_set_client_preview_model_v1.md`, `docs/ui_reporting/briefing_set_builder_implementation_brief_v1.md`, `docs/ui_reporting/guided_briefing_builder_correction_v1.md`, and `docs/ui_reporting/client_briefing_page_model_v1.md`, plus `docs/architecture/simulation_kernel_three_surface_model_v1.md`, `docs/contracts/simulation_kernel_contracts_v1.md`, `docs/contracts/synthetic_position_universe_contract_v1.md`, `docs/contracts/synthetic_market_state_contract_v1.md`, `docs/contracts/simplified_daily_valuation_contract_v1.md`, `docs/contracts/report_element_template_catalog_contract.md`, `docs/contracts/report_element_input_mapping_contract_v1.md`, `docs/contracts/report_element_rendering_contract_v1.md`, `docs/contracts/briefing_set_preview_contract_v1.md`, `docs/contracts/local_briefing_spec_set_contract_v1.md`, `docs/contracts/briefing_spec_set_persistence_contract_v1.md`, `docs/decisions/0003_three_surface_simulation_kernel.md`, `docs/architecture/thin_demo_system_architecture.md`, `docs/architecture/deployable_demo_app_architecture.md`, `docs/architecture/persistence_model_plan.md`, `docs/deployment/private_demo_stack_plan.md`, `docs/decisions/0002_copy_education_private_demo_stack.md`, `docs/architecture/scenario_engine_roadmap.md`, `docs/architecture/data_availability_workstream.md`, `docs/contracts/`, `docs/contracts/workflow_run_persistence_contract.md`, `docs/demo/demo_dataset_plan.md`, `docs/demo/report_workflow_implications.md`, the existing pipeline modules under `src/arangur/`, `src/arangur/simulation/`, `src/arangur/report_elements/`, `src/arangur/app/`, `src/arangur/workflow_templates.py`, `src/arangur/data_coverage.py`, `src/arangur/plaid_mock_adapter.py`, `src/arangur/report_index.py`, `src/arangur/app/static/index.html`, `tests/test_synthetic_position_universe.py`, `tests/test_synthetic_market_state.py`, `tests/test_simplified_daily_valuation.py`, `tests/test_report_element_input_mapping.py`, `tests/test_report_element_rendering.py`, `tests/test_briefing_set_preview.py`, `tests/test_app_health.py`, `tests/test_report_element_catalog.py`, `tests/test_app_runs_api.py`, `tests/test_app_persistence.py`, `tests/test_briefing_spec_set_persistence.py`, `tests/test_app_briefing_spec_sets_api.py`, `data/simulation/synthetic_position_universe_summary.json`, `data/simulation/synthetic_market_state_summary.json`, `data/simulation/simplified_valuation_summary.json`, `data/simulation/report_element_inputs/report_element_input_summary.json`, `data/simulation/report_element_views/report_element_view_summary.json`, `data/simulation/briefing_set_previews/briefing_set_preview_index.json`, and `reports/demo/index.html`.

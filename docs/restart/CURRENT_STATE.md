# Current State

- Current project goal: Build Arangur v2 as a thin end-to-end demo system with replaceable component boundaries.
- Current active workstream: The local workflow/report demo is working, Education-stack alignment has been captured, the FastAPI app serves a lightweight catalog-driven Report Element Finder with dynamic template-specific configuration and manual narrative elements, the Report Element Finder / Composer current model is documented, a static report-element template catalog/API exists, a first optional Postgres persistence skeleton exists, the UI/reporting philosophy now frames Arangur as a portfolio communication and guided briefing system, the three-surface simulation kernel now has Surface 1 implemented as a deterministic synthetic position universe generator, Surface 2 implemented as a deterministic synthetic market/state-of-world generator, Surface 3 implemented as a deterministic simplified daily valuation engine, the first report-element input mapping layer connects those simulation outputs to structured inputs for Portfolio Status, Concentration, Scenario Impact by Manager, Cash Generation Summary, Manager Comparison, and Data Confidence Note, the first report-element rendering layer turns those inputs into simple deterministic view JSON plus Markdown/HTML fragments, the first briefing-set preview layer composes rendered views into deterministic Client Briefing Set and Advisor Review Set previews plus a static preview index, and the browser composer can now match local analytic specs to existing rendered demo views, export/restore the current local spec set, preview the current unsaved ordered sets using existing rendered fragments plus placeholders, print those current local previews, download standalone browser-local HTML previews, and copy plain preview text. The local composer/preview/export surface has passed a focused QA polish pass for calmer hierarchy, clearer local set JSON wording, secondary technical-detail labeling, readable previews, and count-aware restore/export feedback. Persisted briefing sets, generated reports from local composer specs, and full client-ready report generation are not implemented yet.
- Current repo status observed: Arangur v2 has restart docs, thin demo contracts, simulation kernel architecture/contracts, synthetic local JSON fixtures, a standard-library Python demo pipeline under `src/arangur/`, a standard-library simulation package under `src/arangur/simulation/`, deterministic synthetic position universe, market-state, simplified valuation fixtures, report-element input fixtures, report-element view fixtures, and briefing-set preview fixtures under `data/simulation/`, focused tests, generated Markdown/HTML/JSON demo outputs under `reports/demo/`, Plaid-shaped mock outputs under `reports/demo/plaid_mock/`, run metadata and data coverage summaries in report packages, `data_coverage_result.json` artifacts for report runs, a static local report index at `reports/demo/index.html`, workflow-specific report runs under `reports/demo/workflows/` and `reports/demo/plaid_mock/workflows/`, scenario and data availability roadmaps under `docs/architecture/`, deployable-demo design docs for the FastAPI/Postgres/private-demo stack under `docs/architecture/`, `docs/deployment/`, and `docs/decisions/`, UI/reporting philosophy docs under `docs/ui_reporting/`, Briefing Set Builder model docs under `docs/ui_reporting/`, a report-element template catalog, input mapper, renderer, and briefing-set preview builder under `src/arangur/report_elements/` with `/api/report-elements` and `/api/report-elements/{element_id}` endpoints, and a minimal FastAPI app under `src/arangur/app/` with `/api/health`, `/api/sources`, `/api/workflows`, `/api/runs` endpoints, `/api/reports/index`, static serving from `/reports/demo/` and committed synthetic `/simulation/` artifacts, environment-backed settings, and a dependency-free Report Element Finder that fetches `/api/report-elements`, starts with a target-set toggle, keeps the client context to one compact line, uses search/category/topic inputs plus an attached compact grouped `Browse all templates` picker instead of expanded catalog cards or an inline catalog list, shows a template preview before configuration, renders only meaningful selected-template fields, keeps placement separate as `Add to set as`, supports manual narrative elements such as section titles, explanations, prompts, speaker notes, working notes, and follow-up items, adds compact local specs to separate Client Briefing Set and Advisor Review Set lists, marks matching analytic specs as `Preview available`, opens matching rendered HTML fragments in a one-element preview panel, copies/downloads/restores non-empty local set JSON with schema `arangur.local_briefing_spec_set.v1`, keeps technical local export details collapsed by default, previews the current local Client Briefing Set or Advisor Review Set in browser, adds preview-only print/download HTML/copy-text controls after a non-empty current preview is opened, links to static deterministic sample previews, and still does not call workflow APIs or show report-package artifacts in the composer path. The prior fixed-question, dense briefing-set, and card-style catalog UIs should be treated as superseded. Optional Postgres persistence for `workflow_run`, `report_artifact`, and `run_event` remains available when `DB_ENGINE=postgres` and `DATABASE_URL` are set. Default local mode remains `DB_ENGINE=none` and file-backed.
- Last completed batch: Browser-demo readiness QA polish for the local composer/preview/export flow, tests, and docs, completed 2026-06-30.
- Next intended batch: Design/implement backend persistence for briefing spec sets if authorized, or resume Docker/private-demo packaging if Frank decides the local browser demo is ready to package. Do not add charts, live data, production client-ready report generation, or unapproved persistence/Docker changes outside an explicitly authorized follow-up batch.
- Current implementation status for the simulation kernel: Surfaces 1, 2, and 3 are implemented, their outputs feed the first report-element input payloads, those inputs render to simple view JSON plus Markdown/HTML fragments, those views compose into default Client Briefing Set and Advisor Review Set previews, and the browser composer can inspect those pre-rendered views when a local spec matches. Generated simulation report packages from local specs and persisted briefing sets are not implemented yet; local browser export/restore is implemented for demo spec sets.
- Open decisions for Frank: Confirm the actual private-demo subdomain, whether first browser protection should be token-only or cookie/session plus CSRF, and whether generated artifacts should start in a Docker volume or host-mounted app artifact directory.
- Known stop conditions: Do not use real client data; do not inspect credentials or secrets; do not call external APIs; do not integrate live Plaid; do not inspect or port legacy MATLAB without a specific authorized audit batch.
- Files a restarted model should read first: `docs/restart/00_READ_ME_FIRST.md`, then the remaining `docs/restart/` files in numeric order, then `CURRENT_STATE.md`; implementation batches should also read `docs/ui_reporting/`, especially `docs/ui_reporting/report_element_finder_composer_current_model_v1.md`, `docs/ui_reporting/briefing_set_builder_model_v1.md`, `docs/ui_reporting/briefing_set_report_view_model_v1.md`, `docs/ui_reporting/briefing_set_client_preview_model_v1.md`, `docs/ui_reporting/briefing_set_builder_implementation_brief_v1.md`, `docs/ui_reporting/guided_briefing_builder_correction_v1.md`, and `docs/ui_reporting/client_briefing_page_model_v1.md`, plus `docs/architecture/simulation_kernel_three_surface_model_v1.md`, `docs/contracts/simulation_kernel_contracts_v1.md`, `docs/contracts/synthetic_position_universe_contract_v1.md`, `docs/contracts/synthetic_market_state_contract_v1.md`, `docs/contracts/simplified_daily_valuation_contract_v1.md`, `docs/contracts/report_element_template_catalog_contract.md`, `docs/contracts/report_element_input_mapping_contract_v1.md`, `docs/contracts/report_element_rendering_contract_v1.md`, `docs/contracts/briefing_set_preview_contract_v1.md`, `docs/contracts/local_briefing_spec_set_contract_v1.md`, `docs/decisions/0003_three_surface_simulation_kernel.md`, `docs/architecture/thin_demo_system_architecture.md`, `docs/architecture/deployable_demo_app_architecture.md`, `docs/architecture/persistence_model_plan.md`, `docs/deployment/private_demo_stack_plan.md`, `docs/decisions/0002_copy_education_private_demo_stack.md`, `docs/architecture/scenario_engine_roadmap.md`, `docs/architecture/data_availability_workstream.md`, `docs/contracts/`, `docs/contracts/workflow_run_persistence_contract.md`, `docs/demo/demo_dataset_plan.md`, `docs/demo/report_workflow_implications.md`, the existing pipeline modules under `src/arangur/`, `src/arangur/simulation/`, `src/arangur/report_elements/`, `src/arangur/app/`, `src/arangur/workflow_templates.py`, `src/arangur/data_coverage.py`, `src/arangur/plaid_mock_adapter.py`, `src/arangur/report_index.py`, `src/arangur/app/static/index.html`, `tests/test_synthetic_position_universe.py`, `tests/test_synthetic_market_state.py`, `tests/test_simplified_daily_valuation.py`, `tests/test_report_element_input_mapping.py`, `tests/test_report_element_rendering.py`, `tests/test_briefing_set_preview.py`, `tests/test_app_health.py`, `tests/test_report_element_catalog.py`, `tests/test_app_runs_api.py`, `tests/test_app_persistence.py`, `data/simulation/synthetic_position_universe_summary.json`, `data/simulation/synthetic_market_state_summary.json`, `data/simulation/simplified_valuation_summary.json`, `data/simulation/report_element_inputs/report_element_input_summary.json`, `data/simulation/report_element_views/report_element_view_summary.json`, `data/simulation/briefing_set_previews/briefing_set_preview_index.json`, and `reports/demo/index.html`.

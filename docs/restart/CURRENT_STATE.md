# Current State

- Current project goal: Build Arangur v2 as a thin end-to-end demo system with replaceable component boundaries.
- Current active workstream: Local advisor workflow simulation templates are implemented for synthetic demo report generation; scenario engine and data availability roadmaps are captured for future work.
- Current repo status observed: Arangur v2 has restart docs, thin demo contracts, synthetic local JSON fixtures, a standard-library Python demo pipeline under `src/arangur/`, focused tests, generated Markdown/HTML/JSON demo outputs under `reports/demo/`, Plaid-shaped mock outputs under `reports/demo/plaid_mock/`, run metadata in report packages, a static local report index at `reports/demo/index.html`, workflow-specific report runs under `reports/demo/workflows/` and `reports/demo/plaid_mock/workflows/`, a scenario engine roadmap under `docs/architecture/`, and a data availability / valuation-confidence workstream under `docs/architecture/`.
- Last completed batch: Advisor workflow simulation templates, completed 2026-06-27.
- Next intended batch: Data coverage / valuation confidence report prototype, scenario library/source model design, or Plaid Sandbox integration boundary design.
- Open decisions for Frank: Confirm whether data coverage / valuation confidence should become the next visible report prototype, or whether scenario source modeling or Plaid Sandbox boundary design should come first.
- Known stop conditions: Do not use real client data; do not inspect credentials or secrets; do not call external APIs; do not integrate live Plaid; do not inspect or port legacy MATLAB without a specific authorized audit batch.
- Files a restarted model should read first: `docs/restart/00_READ_ME_FIRST.md`, then the remaining `docs/restart/` files in numeric order, then `CURRENT_STATE.md`; implementation batches should also read `docs/architecture/thin_demo_system_architecture.md`, `docs/architecture/scenario_engine_roadmap.md`, `docs/architecture/data_availability_workstream.md`, `docs/contracts/`, `docs/demo/demo_dataset_plan.md`, `docs/demo/report_workflow_implications.md`, the existing pipeline modules under `src/arangur/`, `src/arangur/workflow_templates.py`, `src/arangur/plaid_mock_adapter.py`, `src/arangur/report_index.py`, and `reports/demo/index.html`.

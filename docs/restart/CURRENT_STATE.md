# Current State

- Current project goal: Build Arangur v2 as a thin end-to-end demo system with replaceable component boundaries.
- Current active workstream: The local workflow/report demo is working, Education-stack alignment has been captured, the FastAPI app serves a lightweight catalog-driven Report Element Finder with dynamic template-specific configuration and manual narrative elements, the Report Element Finder / Composer current model is documented, a static report-element template catalog/API exists, optional Postgres persistence skeletons cover workflow run metadata and draft briefing spec sets, the local Docker/private-demo stack has been runtime-smoked by Frank with the FastAPI app plus internal Postgres service, and generated report artifacts now sit behind a small demo population service/API. The advisor-facing browser composer opens on a sparse Advisor Home with exactly four product choices: Create a new workflow, Work with an existing workflow, Populate a workflow with data, and Present / view reports. Static hash routes cover Advisor Home, workflow choosers, Builder, Populate, Presentation view, Client Preview, Advisor Preview, and Developer / QA tools so browser Back stays inside the product path. Browser-local named workflows use `localStorage` key `arangur.local_named_briefing_workflows.v1`; saved records preserve a human-readable `display_name` separately from an internal `workflow_id` / `storage_key`, include local timestamps and a simple workflow type label, and keep Save, Rename, Save As New, and Copy as distinct non-destructive actions. Saved workflows can be opened to edit, deleted, and populated with current demo data without creating backend records or generated report history. Populate now resolves the selected saved workflow by `workflow_id` at report-creation time, shows a small source-workflow confirmation, calls `POST /api/generated-reports/demo-populate` with that saved payload and `Current synthetic demo snapshot` / `2026-06-30`, opens a clean demo populated report view with advisor-authored workflow sections, concise metadata/caveat footer, Back to Home, Back to Workflow, Print, Export HTML, and Copy text, and stores the returned artifact only in this browser under `localStorage` key `arangur.local_generated_reports.v1`. Generated report titles derive from the selected workflow display name, and generated report IDs include a per-population request suffix so repeated Populate actions do not overwrite unrelated shelf records. Present / view reports now lists those browser-local generated report artifacts with source workflow, local generated timestamp, and data snapshot label; opens a clean Generated report presentation view with source workflow, report type, generated/data-as-of metadata, Back to Home, Back to Reports, Print, Export HTML, and Copy text; and can delete only the shelf record without deleting the source workflow. Generated Client Briefing and Advisor Review artifacts preserve the advisor-authored workflow body sequence, narrative/title text, selected template titles, and matched rendered fragment detail; they no longer auto-add framing, closing prompts, follow-up sections, or caveat body sections. The Builder now exposes curated approved-pack choices for supported templates, including Strategic Theme, Manager Role / Mandate, Liquidity Profile, Data Confidence, approved theme focus labels, and the five published scenario names. Preview and Populate can resolve supported configured specs to analytic-derived fragments for Portfolio Status, Concentration, Manager Comparison, Scenario Impact by Manager for AI / Chip Selloff, and Data Confidence Note while keeping unsupported selections as clean placeholders. A concise end-to-end private-demo walkthrough and QA checklist now documents the local start/smoke/app/click-through/stop flow for Frank or another demo operator. Local workflow JSON transfer, backend draft save/load test controls, QA reference previews, and a small clear-local-generated-reports action are demoted into Developer / QA tools. Demo-rendered analytic elements still guard or hide scope choices that the committed demo preview cannot honestly reflect; Cash Generation Summary previews only at the whole-portfolio cash-summary level. Backend generated report history, report library UI, production report history, durable backend artifact persistence, and client-ready report generation are not implemented yet.
- Current repo status observed: Arangur v2 has restart docs, thin demo contracts, simulation kernel architecture/contracts, synthetic local JSON fixtures, a standard-library Python demo pipeline under `src/arangur/`, a standard-library simulation package under `src/arangur/simulation/`, deterministic synthetic position universe, market-state, simplified valuation fixtures, report-element input/view fixtures and briefing-set preview fixtures under `data/simulation/`, focused tests, generated demo outputs under `reports/demo/`, and a minimal FastAPI app under `src/arangur/app/`. The dependency-free browser composer starts at Advisor Home, fetches `/api/report-elements` in the Builder workspace, keeps the client context to one compact line, uses search/category/topic inputs plus an attached compact grouped `Browse all templates` picker, shows a template preview before configuration, renders only meaningful selected-template fields, supports manual narrative elements, adds compact local specs to separate Client Briefing Workflow and Advisor Review Workflow lists, labels matching analytic specs as previewable, keeps one-element preview as a secondary element tool, copies/downloads/restores non-empty local workflow JSON with schema `arangur.local_briefing_spec_set.v1` from Developer / QA tools, previews current unsaved ordered workflows in browser using existing rendered fragments plus nontechnical placeholders, exposes preview-only print/export/copy controls after a non-empty current preview is opened, optionally saves/loads draft local spec-set payloads through Developer / QA backend controls, keeps QA reference preview links under Developer / QA tools, separates existing-workflow chooser actions into edit/populate modes, separates Present into a browser-local generated report shelf, and uses the generated-report populate endpoint only from the Populate path. The composer path still does not call `/api/runs`, show report-package artifacts, expose raw generated artifact JSON, or add search/filter/version report-library chrome in the advisor path. The prior fixed-question, dense briefing-set, card-style catalog, vague saved-set home, flat multi-action Advisor Home UI, and all-saved-actions-open-builder behavior should be treated as superseded. Optional Postgres persistence for `workflow_run`, `report_artifact`, `run_event`, `briefing_spec_set`, and `briefing_spec_item` remains available when `DB_ENGINE=postgres` and `DATABASE_URL` are set, but demo generated report population does not require Postgres or create new database records. Default local mode remains `DB_ENGINE=none`, file-backed for workflow runs, and not-configured/no-op for briefing spec-set save attempts.
- Last completed implementation batch: Synthetic Attribution Report Mockups v1, completed 2026-07-07 in this checkpoint. `src/arangur/analytics/attribution_report_views.py` now generates deterministic local-only attribution report input fixtures, view fixtures, and Markdown product-review mockups from Synthetic Attribution Prerequisite Pack v1. This adds Integrated Performance Attribution Summary/Detail, Manager Attribution Summary, and Lens-Based Performance Attribution mockups for AI Adoption and Energy Security. It does not add advisor UI, Advisor Preview/Populate/Present/generated-report wiring, backend endpoints, Docker/deployment changes, production attribution math, live/real data, external APIs, or dependencies.
- Last manual checkpoint: Frank reran the local private-demo browser rehearsal after commit `524a99b` and passed the previously failing identity/rendering case. Docker stack startup, smoke script, authored workflow preview, copied-workflow Populate, generated report sequence, removal of automatic framing/closing sections, and Present / view reports opening were manually verified. The prior Populate/generated-report mismatch is fixed. The Docker/private-demo stack remains local only and is not a public deployment.
- Current architecture direction: Arranger now has an explicit internal analytic control-plane boundary. The Arranger Internal Analytic Studio / Control Plane publishes approved analytic packs containing theme catalogs, classification lens catalogs, scenario catalogs, scenario shock packs, data confidence rules, and report analytic capability maps. The Arangur Advisor App consumes approved packs, lets advisors select curated scenarios/themes/lenses in workflows and reports, and must not expose control-plane construction tools as advisor-facing UI. The first meaningful pack content now lives in `data/analytic_packs/arranger_demo_pack_v1/`, with the boundary/contract docs at `docs/architecture/arranger_control_plane_boundary_v1.md` and `docs/contracts/analytic_pack_contract_v1.md`; the local Advisor workflow now consumes that pack through template metadata and committed analytic-derived report views.
- Last completed analytic-foundation batch: Published and applied the first Arranger Demo Analytic Pack v1. The pack now has 12 curated themes, 4 classification lenses, 5 supported synthetic scenarios, matching shock definitions, 6 data confidence rules, and report capability coverage for the six current report element ids. `src/arangur/analytics/analytic_pack_loader.py` loads/summarizes/validates the pack, and `src/arangur/analytics/apply_demo_pack.py` applies it to the existing synthetic portfolio/scenario/valuation fixtures to write deterministic proof outputs under `data/simulation/analytics/`: theme exposure, manager/theme overlap, scenario impact by theme/manager, data confidence map, cross-scenario resilience summary, and output index. This did not add advisor UI, Arranger Studio UI, backend endpoints, generated-report wiring, Docker/deployment changes, simulation-kernel changes, live data, external APIs, real data, or dependencies.
- Last completed analytic-consumption batch: Wired approved analytic pack choices and analytic-derived report views into the existing Advisor workflow path. `templates.json` now exposes curated pack-driven lens/theme/scenario/confidence choices for the relevant report elements; the static app reads both legacy and analytic rendered-view summaries; Preview resolves supported configured specs to analytic fragments; Populate recomputes view matches from saved configured parameters before trusting stored match metadata; and generated report artifacts carry a small selected-configuration note without exposing pack JSON, control-plane tooling, or raw debug output. Unsupported choices such as Rate Shock for Scenario Impact by Manager remain honest placeholders until a matching committed fragment exists. Cash Generation Summary remains on the existing simulation-derived fixture path because the analytic proof outputs did not add a simpler cash-specific source.
- Latest analytic demonstration checkpoint: Analytic Report Demonstration Pack v1 now has advisor-readable rendered fragments with explicit takeaway sections for Portfolio Status, Concentration, Manager Comparison, Scenario Impact, and Data Confidence. The fragments use existing synthetic proof outputs only, keep caveats short, and surface hidden concentration, manager overlap, AI / Chip Selloff scenario vulnerability, data-confidence review needs, and cross-scenario resilience signals without exposing control-plane construction language. The canonical local story and restoreable browser-local workflow fixture live at `docs/demo/analytic_report_demo_story_v1.md` and `docs/demo/analytic_demo_workflow_fixture.json`; they are intended for one clean local browser rehearsal through Preview, Populate, and Present.
- Latest analytic architecture checkpoint: `docs/architecture/arranger_internal_analytics_algorithm_design_v1.md` now defines full portfolio revaluation as the controlling scenario methodology. Arranger scenarios construct complete scenario market states from approved key-rate/driver perturbations; the engine values every position under the base market state and scenario market state; impact is scenario value minus base value; and attribution by position, manager, account, sleeve, theme, scenario, and confidence happens after valuation. Themes are classification/reporting layers, not valuation substitutes. Coverage/confidence language now describes revaluation limitations, substitute-input use, approved mark policies, and review-required treatment. This is documentation-only design work; it does not add Arranger Studio UI, advisor controls, backend endpoints, scenario-engine runtime, covariance/PCA runtime, Docker/deployment changes, live data, external APIs, real data, dependencies, or product behavior changes.
- Latest scenario-basis design checkpoint: `docs/architecture/key_market_state_scenario_basis_design_v1.md` defines key market-state scenario basis models as small, curated, price-like coordinate systems for constructing complete scenario market states. It supports multiple basis models rather than one universal key-rate set, including broad multi-asset, rates/credit/curve, equity/growth/AI, commodity/energy, FX/global macro, and private liquidity/alternatives examples. Basis construction extracts a base basis vector, applies anchor shocks, completes the vector deterministically or with reduced covariance/PCA, validates it, expands it to full market inputs, and stops before full portfolio revaluation begins.
- Latest thesis-lens design checkpoint: `docs/architecture/thesis_lens_position_mapping_design_v1.md` defines complete thesis-specific position mapping schemas for lenses such as AI Adoption, Deglobalization, Geopolitical Bloc, Energy Security, Credit Stress, and Private Liquidity. V1 uses one mutually exclusive primary bucket per in-scope position per thesis lens, optional non-additive secondary flags, neutral/review buckets, a deterministic classification precedence stack, internal LLM-assisted classification policy, published mapping artifacts, and post-valuation aggregation by thesis bucket. Thesis lenses classify already-valued positions and impacts; they do not price positions, construct scenario market states, or replace full revaluation.
- Latest valuation-coverage design checkpoint: `docs/architecture/position_valuation_coverage_mapping_design_v1.md` defines how positions map to instruments, pricing functions, required market inputs, valuation results, and explicit coverage status. It documents a universal pricing-function interface, instrument and position schemas, pricing-function registry selection, required market input mapping, coverage statuses, asset-class treatment examples, base/scenario comparison, governance, published valuation coverage artifacts, and advisor-facing consumption rules. Pricing functions consume complete base/scenario market states, and coverage limitations become confidence/caveat/review outputs rather than alternate heuristic impact formulas.
- Latest analytic implementation checkpoint: Synthetic Full Revaluation Engine Skeleton v1 now supports two deterministic full-revaluation scenarios: `ai_chip_selloff` and `rate_shock`. `src/arangur/analytics/pricing_functions.py` and `src/arangur/analytics/revaluation.py` derive instrument, position, pricing-function, market-state, coverage, valuation, comparison, portfolio-summary, coverage-manifest, bundle-manifest, and scenario-index artifacts from the synthetic position universe and synthetic scenario market-state fixtures. `python -m arangur.analytics.revaluation_runner` now regenerates all supported scenarios by default and writes deterministic outputs under `data/simulation/revaluation/`, preserving the existing `ai_chip_selloff` filenames while adding `rate_shock` files plus `revaluation_scenario_index.json`. Scenario impact remains computed as scenario portfolio value minus base portfolio value after every position is valued or explicitly routed to review; it does not reuse the old simplified scenario impact fixture as a valuation source. This did not add advisor UI, report views, backend endpoints, Docker/deployment changes, live data, external APIs, real data, dependencies, production pricing, covariance/PCA runtime, or pushes.
- Latest revaluation-attribution checkpoint: Revaluation-Based Attribution Outputs now run for both `ai_chip_selloff` and `rate_shock`. `src/arangur/analytics/revaluation_attribution.py` writes deterministic manager, account, sleeve, coverage, confidence, gross theme, thesis-readiness, cross-scenario summary, cross-scenario readiness, and attribution-index outputs under `data/simulation/revaluation/attribution/`. Manager/account/sleeve and coverage/confidence outputs reconcile to each scenario's full revaluation portfolio summary. Theme attribution remains limited and non-additive because it uses existing synthetic position tags/classifications and approved pack theme matchers. Thesis-bucket attribution remains readiness-only until published position-thesis assignments exist. A real `cross_scenario_revaluation_summary.json` now derives from the two full-revaluation comparison outputs only; legacy proof scenario-impact outputs are not mixed in. This did not add advisor UI, report views, generated-report wiring, backend endpoints, Docker/deployment changes, live data, external APIs, real data, dependencies, production pricing, covariance/PCA runtime, or pushes.
- Latest report-purpose checkpoint: `docs/product/report_purpose_information_economy_audit_v1.md` now defines the reporting information economy before revaluation outputs are mapped into report elements. Reports are organized around four master question families: Ownership / Exposure Explanation, Performance / Plan Explanation, Risk / Downside Explanation, and Positioning / Forward View Explanation. The audit states that every visible report item must earn its place, warns against analytics tourism, inventories current and planned report elements, audits full-revaluation outputs by audience tier, recommends a lean first report set, defines suppression rules, and separates build-now, defer, advisor-only, internal-only, and kill/merge choices. `docs/contracts/report_element_information_budget_v1.md` adds a non-binding design contract for future report-element budgets and anti-clutter checks.
- Latest lean report mockup checkpoint: `src/arangur/analytics/lean_report_views.py` now maps the two-scenario full-revaluation outputs and revaluation attribution layer into lean report input fixtures, report view fixtures, and Markdown product mockups for Portfolio Status, Aggregated Asset Allocation, Manager Role Summary, Concentration Review, Scenario Downside Summary, Coverage and Confidence Warning, and a gated Cash-Flow Support Readiness note. The fixtures live under `data/simulation/report_element_inputs/lean_revaluation_v1/` and `data/simulation/report_element_views/lean_revaluation_v1/`; the product-review mockups live under `docs/product/report_mockups/` and are generated from the same view fixtures. Information budgets are enforced in tests: max one headline sentence, three headline metrics, five table rows, two caveats, and one advisor note. Cash-Flow Support remains readiness-only because no explicit cash need, liability schedule, or approved funding source is present. This did not add advisor UI, report wiring, backend endpoints, Docker/deployment changes, live data, external APIs, real data, dependencies, or pushes.
- Latest report-system redesign checkpoint: `docs/product/report_system_redesign_blueprint_v1.md`, `docs/product/report_family_catalog_v1.md`, and `docs/architecture/report_data_and_analytics_requirements_v1.md` now consolidate Frank's product review of the lean report mockups into a design-only report-system blueprint. The docs keep the current implementation paused while defining report families, current mockup redesign decisions, representation-level rules, lens and benchmark design, integrated performance attribution, probabilistic scenario reports, data/analytics readiness gates, and build-now/design-soon/defer sequencing. `docs/contracts/report_element_information_budget_v1.md` now adds supporting design metadata constraints for representation level, denominator/category system, summary/detail variants, lens/benchmark readiness, visual ranges, and timing-attribution gates. This did not add source code, app behavior, advisor UI, report views, generated-report wiring, analytics outputs, tests, backend endpoints, Docker/deployment changes, live data, real data, dependencies, or pushes.
- Latest revised mockup spec checkpoint: `docs/product/revised_report_mockup_spec_v2.md` now translates the Report System Redesign Blueprint into a concrete product-facing specification for the next generation of Markdown report mockups. It documents the v1 mockup dispositions, build-now v2 mockups, gated/deferred mockups, visual-first mockup concepts, lens/benchmark implications, integrated performance attribution implications, prerequisite gates, information budgets, review checklist, and next implementation outline. This tranche did not regenerate mockups, regenerate fixtures, generate analytics outputs, alter tests, change source code, change advisor UI, wire reports into Preview/Populate/Present, add endpoints, or change runtime behavior.
- Latest synthetic prerequisite checkpoint: Synthetic Report Prerequisite Pack v1 now exists at `data/simulation/report_prerequisites/synthetic_report_prerequisite_pack_v1/`. `src/arangur/analytics/synthetic_report_prerequisites.py` generates deterministic local-only prerequisite artifacts for whole-portfolio Cash-Flow Support Summary, approved synthetic manager mandate language, complete AI Adoption and Energy Security lens definitions, one primary lens assignment for every in-scope synthetic position, lens exposure readiness, and scenario/lens readiness notes. The pack makes Full Lens Exposure honest for the synthetic AI Adoption and Energy Security lenses, makes Manager Role Summary use an approved synthetic mandate catalog, and gives Cash-Flow Support Summary explicit synthetic need/generated/paid/projected/surplus inputs instead of missing-input readiness. It does not wire Advisor Preview, Populate, Present, or generated reports; does not add backend endpoints, Docker/deployment changes, external APIs, live/real data, benchmark maps, performance attribution, probabilistic ranges, proposed-portfolio analytics, or production cash-flow forecasting.
- Latest synthetic attribution prerequisite checkpoint: Synthetic Attribution Prerequisite Pack v1 now exists at `data/simulation/attribution_prerequisites/synthetic_attribution_prerequisite_pack_v1/`. `src/arangur/analytics/synthetic_attribution_prerequisites.py` generates deterministic local-only artifacts for a synthetic policy benchmark, AI Adoption and Energy Security lens-bucket proxy map, synthetic period returns, manager/lens/proxy weights, simple no-external-flow context, whole-portfolio and manager-by-manager decomposition inputs, manager attribution prerequisites, and attribution readiness. The pack made Integrated Performance Attribution Summary/Detail, Manager Attribution Summary, and Lens-Based Performance Attribution eligible for local synthetic demo mockups; those mockups now exist in `attribution_v1`. Timing remains unavailable rather than used as a residual, and Scenario Versus Benchmark, Probabilistic Scenario Range, Current Versus Proposed Portfolio, production/client attribution, Advisor Preview/Populate/Present wiring, generated reports, backend endpoints, Docker/deployment, live/real data, external APIs, and dependencies remain out of scope.
- Latest synthetic attribution mockup checkpoint: Synthetic Attribution Report Mockups v1 now exist under `data/simulation/report_element_inputs/attribution_v1/`, `data/simulation/report_element_views/attribution_v1/`, and `docs/product/report_mockups/attribution_v1/`. `python -m arangur.analytics.attribution_report_views` reads Synthetic Attribution Prerequisite Pack v1 and writes five build-now local product-review reports: Integrated Performance Attribution Summary, Integrated Performance Attribution Detail, Manager Attribution Summary, Lens-Based Performance Attribution - AI Adoption, and Lens-Based Performance Attribution - Energy Security. Markdown mockups are rendered from view fixtures rather than hand-authored. The views enforce one headline sentence, three headline metrics, bounded rows, two caveats, and one advisor note; manager summary shows all six current managers; lens reports show all seven buckets; residual is labeled `Residual / unexplained`; timing remains unavailable and is not used as a contribution. The gated/deferred index keeps Timing Attribution, Production Attribution Report, Scenario Versus Benchmark, Probabilistic Scenario Range, and Current Versus Proposed Attribution ungenerated.
- Latest v2 report mockup checkpoint: `src/arangur/analytics/lean_report_views_v2.py` now generates concrete v2 report input fixtures under `data/simulation/report_element_inputs/revaluation_v2/`, v2 report view fixtures under `data/simulation/report_element_views/revaluation_v2/`, and Markdown product-review mockups under `docs/product/report_mockups/revaluation_v2/`. The 14 generated v2 mockups are produced from the view fixtures and cover Portfolio Representation Status, Aggregated Asset Allocation, Allocation by Manager, Coverage and Confidence Warning, Concentration by Asset Type, Concentration by Manager/Sleeve, Current Portfolio Scenario Downside, Manager Role Summary, Cash Flow Delivered, Cash-Flow Support Outlook, Full Lens Exposure for AI Adoption and Energy Security, and Manager by Lens Exposure for AI Adoption and Energy Security. The latest content-polish tranche split backward-looking delivered cash from forward-looking cash support, renamed the support report as Cash-Flow Support Outlook, clarified grouped manager/sleeve rows, and marks attribution and probabilistic range reports as design-soon / prerequisite-soon in the gated index. The v2 generator uses Synthetic Report Prerequisite Pack v1 for cash-flow delivered/outlook inputs, approved manager role language, full lens exposure, and manager-by-lens exposure; writes a gated/deferred report index for benchmark, performance attribution, probabilistic range, current-versus-proposed, timing attribution, and custom benchmark construction; and leaves Advisor Preview/Populate/Present, generated reports, backend endpoints, Docker/deployment, live/real data, dependencies, production cash-flow forecasting, benchmark analytics, performance attribution, probabilistic analytics, and proposed-allocation workflows untouched.
- Next decision: Frank should review `docs/product/report_mockups/attribution_v1/` alongside the existing v2 revaluation mockups, then choose either attribution content polish, broader v2 content polish, or Scenario by Lens aggregation using existing full-revaluation rows plus complete synthetic lens assignments. Advisor Preview/Populate/Present wiring and generated-report integration should remain paused until product review approves the report shapes. Private-server deployment planning remains parked.
- Current implementation status for the simulation kernel: Surfaces 1, 2, and 3 are implemented, their outputs feed the first report-element input payloads, those inputs render to simple view JSON plus Markdown/HTML fragments, those views compose into default Client Briefing Set and Advisor Review Set preview fixtures, and the generated report artifact layer can wrap those previews or selected browser-local workflow specs into dated synthetic artifact objects. The Arranger demo analytic pack proof layer consumes those existing synthetic outputs, writes separate deterministic analytics JSON under `data/simulation/analytics/`, maps those analytics outputs into separate analytic report-element input/view fixtures, and the local Advisor workflow can consume those analytic views for supported configured specs. The newer lean revaluation layer writes local product-review fixtures and Markdown mockups only; it is not yet wired into Advisor Preview, Populate, Present, or generated reports. Generated simulation report packages from arbitrary local composer specs are not implemented; Populate creates a generated report artifact JSON response, browser-local shelf record, and advisor-facing view only. Local browser workflow JSON export/restore, browser-local named workflow saving, and the browser-local generated report shelf are implemented for demo draft/product-path modeling, and optional backend persistence can save/load draft spec-set payloads without generating reports.
- Open decisions for Frank: Decide whether the next product/mockup tranche should polish the new `attribution_v1` mockups, continue reviewing/polishing the existing v2 revaluation mockups, or aggregate Scenario by Lens. Also decide later whether any approved mockups are ready for a separate future Advisor Preview/Populate/Present wiring tranche. Later deployment decisions remain parked: private-demo subdomain, first browser protection, generated artifact storage location, and whether named workflows should remain browser-local drafts or graduate to durable backend records.
- Known stop conditions: Do not use real client data; do not inspect credentials or secrets; do not call external APIs; do not integrate live Plaid; do not inspect or port legacy MATLAB without a specific authorized audit batch.
- Files a restarted model should read first: `docs/restart/00_READ_ME_FIRST.md`, then the remaining `docs/restart/` files in numeric order, then `CURRENT_STATE.md`; implementation batches should also read `docs/ui_reporting/`, especially `docs/ui_reporting/report_element_finder_composer_current_model_v1.md`, `docs/ui_reporting/briefing_set_builder_model_v1.md`, `docs/ui_reporting/briefing_set_report_view_model_v1.md`, `docs/ui_reporting/briefing_set_client_preview_model_v1.md`, `docs/ui_reporting/briefing_set_builder_implementation_brief_v1.md`, `docs/ui_reporting/guided_briefing_builder_correction_v1.md`, and `docs/ui_reporting/client_briefing_page_model_v1.md`, plus `docs/architecture/arranger_internal_analytics_algorithm_design_v1.md`, `docs/architecture/key_market_state_scenario_basis_design_v1.md`, `docs/architecture/thesis_lens_position_mapping_design_v1.md`, `docs/architecture/position_valuation_coverage_mapping_design_v1.md`, `docs/contracts/report_element_template_catalog_contract.md`, `docs/contracts/local_briefing_spec_set_contract_v1.md`, `docs/contracts/briefing_spec_set_persistence_contract_v1.md`, `docs/contracts/generated_report_artifact_contract_v1.md`, the existing pipeline modules under `src/arangur/`, `src/arangur/report_elements/`, `src/arangur/app/`, `src/arangur/app/static/index.html`, and focused app/UI tests such as `tests/test_app_health.py`, `tests/test_report_element_catalog.py`, `tests/test_app_runs_api.py`, `tests/test_app_persistence.py`, `tests/test_briefing_spec_set_persistence.py`, `tests/test_app_briefing_spec_sets_api.py`, and `tests/test_generated_report_artifact.py`.

# Current State

- Current project goal: Build Arangur v2 as a thin end-to-end demo system with replaceable component boundaries.
- Current active workstream: The local workflow/report demo is working, Education-stack alignment has been captured, and the FastAPI app can wrap local workflow runs.
- Current repo status observed: Arangur v2 has restart docs, thin demo contracts, synthetic local JSON fixtures, a standard-library Python demo pipeline under `src/arangur/`, focused tests, generated Markdown/HTML/JSON demo outputs under `reports/demo/`, Plaid-shaped mock outputs under `reports/demo/plaid_mock/`, run metadata and data coverage summaries in report packages, `data_coverage_result.json` artifacts for report runs, a static local report index at `reports/demo/index.html`, workflow-specific report runs under `reports/demo/workflows/` and `reports/demo/plaid_mock/workflows/`, scenario and data availability roadmaps under `docs/architecture/`, deployable-demo design docs for the FastAPI/Postgres/private-demo stack under `docs/architecture/`, `docs/deployment/`, and `docs/decisions/`, and a minimal FastAPI app under `src/arangur/app/` with `/api/health`, `/api/sources`, `/api/workflows`, file-backed `/api/runs` endpoints, `/api/reports/index`, static serving from `/reports/demo/`, environment-backed settings, and a static placeholder demo console.
- Last completed batch: FastAPI workflow run endpoints, completed 2026-06-27.
- Next intended batch: Browser UI source/workflow selector, Postgres persistence skeleton, or Docker Compose private-demo files.
- Open decisions for Frank: Confirm the actual private-demo subdomain, whether first browser protection should be token-only or cookie/session plus CSRF, and whether generated artifacts should start in a Docker volume or host-mounted app artifact directory.
- Known stop conditions: Do not use real client data; do not inspect credentials or secrets; do not call external APIs; do not integrate live Plaid; do not inspect or port legacy MATLAB without a specific authorized audit batch.
- Files a restarted model should read first: `docs/restart/00_READ_ME_FIRST.md`, then the remaining `docs/restart/` files in numeric order, then `CURRENT_STATE.md`; implementation batches should also read `docs/architecture/thin_demo_system_architecture.md`, `docs/architecture/deployable_demo_app_architecture.md`, `docs/architecture/persistence_model_plan.md`, `docs/deployment/private_demo_stack_plan.md`, `docs/decisions/0002_copy_education_private_demo_stack.md`, `docs/architecture/scenario_engine_roadmap.md`, `docs/architecture/data_availability_workstream.md`, `docs/contracts/`, `docs/demo/demo_dataset_plan.md`, `docs/demo/report_workflow_implications.md`, the existing pipeline modules under `src/arangur/`, `src/arangur/app/`, `src/arangur/workflow_templates.py`, `src/arangur/data_coverage.py`, `src/arangur/plaid_mock_adapter.py`, `src/arangur/report_index.py`, `tests/test_app_health.py`, `tests/test_app_runs_api.py`, and `reports/demo/index.html`.

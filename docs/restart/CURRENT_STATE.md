# Current State

- Current project goal: Build Arangur v2 as a thin end-to-end demo system with replaceable component boundaries.
- Current active workstream: The local workflow/report demo is working, Education-stack alignment has been captured, the FastAPI app can wrap local workflow runs from a browser briefing console, a first optional Postgres persistence skeleton exists, and the UI/reporting philosophy now frames Arangur as a portfolio communication and guided briefing system.
- Current repo status observed: Arangur v2 has restart docs, thin demo contracts, synthetic local JSON fixtures, a standard-library Python demo pipeline under `src/arangur/`, focused tests, generated Markdown/HTML/JSON demo outputs under `reports/demo/`, Plaid-shaped mock outputs under `reports/demo/plaid_mock/`, run metadata and data coverage summaries in report packages, `data_coverage_result.json` artifacts for report runs, a static local report index at `reports/demo/index.html`, workflow-specific report runs under `reports/demo/workflows/` and `reports/demo/plaid_mock/workflows/`, scenario and data availability roadmaps under `docs/architecture/`, deployable-demo design docs for the FastAPI/Postgres/private-demo stack under `docs/architecture/`, `docs/deployment/`, and `docs/decisions/`, UI/reporting philosophy docs under `docs/ui_reporting/`, and a minimal FastAPI app under `src/arangur/app/` with `/api/health`, `/api/sources`, `/api/workflows`, `/api/runs` endpoints, `/api/reports/index`, static serving from `/reports/demo/`, environment-backed settings, a dependency-free browser briefing console organized around client question, audience depth, briefing preparation, report links, and recent briefings, and optional Postgres persistence for `workflow_run`, `report_artifact`, and `run_event` when `DB_ENGINE=postgres` and `DATABASE_URL` are set. Default local mode remains `DB_ENGINE=none` and file-backed. UI correction captured: the current browser console is functional but too dense, and the target UI is a sparse guided briefing builder with a separate client briefing page plus evidence and technical/admin appendices.
- Last completed batch: Guided briefing builder correction, completed 2026-06-28.
- Next intended batch: Replace the dense browser briefing console with a sparse guided briefing builder, add a separate client briefing page surface, add advisor draft review, move technical links/run history into a technical/admin appendix, then resume backend/client-question metadata or Docker/Postgres private-demo path.
- Open decisions for Frank: Confirm the actual private-demo subdomain, whether first browser protection should be token-only or cookie/session plus CSRF, and whether generated artifacts should start in a Docker volume or host-mounted app artifact directory.
- Known stop conditions: Do not use real client data; do not inspect credentials or secrets; do not call external APIs; do not integrate live Plaid; do not inspect or port legacy MATLAB without a specific authorized audit batch.
- Files a restarted model should read first: `docs/restart/00_READ_ME_FIRST.md`, then the remaining `docs/restart/` files in numeric order, then `CURRENT_STATE.md`; implementation batches should also read `docs/ui_reporting/`, especially `docs/ui_reporting/guided_briefing_builder_correction_v1.md` and `docs/ui_reporting/client_briefing_page_model_v1.md`, plus `docs/architecture/thin_demo_system_architecture.md`, `docs/architecture/deployable_demo_app_architecture.md`, `docs/architecture/persistence_model_plan.md`, `docs/deployment/private_demo_stack_plan.md`, `docs/decisions/0002_copy_education_private_demo_stack.md`, `docs/architecture/scenario_engine_roadmap.md`, `docs/architecture/data_availability_workstream.md`, `docs/contracts/`, `docs/contracts/workflow_run_persistence_contract.md`, `docs/demo/demo_dataset_plan.md`, `docs/demo/report_workflow_implications.md`, the existing pipeline modules under `src/arangur/`, `src/arangur/app/`, `src/arangur/workflow_templates.py`, `src/arangur/data_coverage.py`, `src/arangur/plaid_mock_adapter.py`, `src/arangur/report_index.py`, `src/arangur/app/static/index.html`, `tests/test_app_health.py`, `tests/test_app_runs_api.py`, `tests/test_app_persistence.py`, and `reports/demo/index.html`.
